[project]
name = "agent-playbook-suite"

[archive]
dir = "archive"
date_format = "%Y-%m-%d"

[exclude]
dirs = [
    "site",
    ".github",
    ".agents",
    ".claude-plugin",
    "plugins",
]
globs = []
exts = []
